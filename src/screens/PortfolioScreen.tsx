import React from 'react';
import { View } from 'react-native';

// Screen to show user's positions across all vaults
export default function PortfolioScreen() {
  // TODO: add total value and per-vault breakdown
  return <View />;
}

