import React from 'react';
import { View } from 'react-native';

// Screen for the Approve -> Deposit flow into a vault
export default function DepositScreen() {
  // TODO: add amount input, quick buttons, and transaction steps
  return <View />;
}

