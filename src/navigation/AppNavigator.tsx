import React from 'react';
import { View } from 'react-native';

// Root navigator for the app (we'll wire this up later)
export default function AppNavigator() {
  // TODO: add navigation container and stacks
  return <View />;
}



