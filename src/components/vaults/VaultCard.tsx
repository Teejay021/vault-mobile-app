import React from 'react';
import { View } from 'react-native';

// Card that will display vault info (name, risk, APY, TVL, user balance)
export default function VaultCard() {
  // TODO: add props and layout
  return <View />;
}

